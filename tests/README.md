# Test Suite

The frontend tests use Vitest, jsdom, and React Testing Library.

## Commands

```bash
npm test
npm run test:watch
npm run test:coverage
```

The suite covers reusable pagination, agent filtering and sorting, and the
nullable bio fallback used by featured agent cards.